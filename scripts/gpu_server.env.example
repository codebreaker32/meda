# Copy to scripts/gpu_server.env (not committed) and fill in your server;
# scripts/run_on_gpu_server.sh reads it.
GPU_SERVER=student@192.168.1.50      # user@IP of the lab GPU server
REMOTE_DIR=meda-gnn-routing          # folder in the server user's home
REMOTE_PY=python3                    # python on the server
SSH_OPTS=""                          # e.g. "-p 2222 -i ~/.ssh/lab_key"
# MEDA_DEVICE=cuda:1                 # pick a specific GPU (default: most free memory)
# TORCH_INDEX=https://download.pytorch.org/whl/cu126   # PyTorch build matching the server driver (nvidia-smi)
